{
  "name": "@apps/{{APP_NAME}}",
  "version": "{{VERSION}}",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:local": "vite --mode development",
    "dev:test": "vite --mode test",
    "dev:uat": "vite --mode uat",
    "build": "vue-tsc -b && vite build",
    "build:local": "vue-tsc -b && vite build --mode development",
    "build:test": "vue-tsc -b && vite build --mode test",
    "build:uat": "vue-tsc -b && vite build --mode uat",
    "analyze": "vue-tsc -b && cross-env BUILD_ANALYZE=true vite build",
    "lint": "eslint .",
    "typecheck": "vue-tsc -b",
    "test": "vitest run --passWithNoTests",
    "test:coverage": "vitest run --coverage",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@repo/config": "workspace:*",
    "@repo/observability": "workspace:*",
    "@repo/request": "workspace:*",
    "@repo/utils": "workspace:*",
    "@repo/vue-ui": "workspace:*",
    "pinia": "catalog:",
    "vue": "catalog:",
    "vue-router": "catalog:"
  },
  "devDependencies": {
    "@repo/tsconfig": "workspace:*",
    "@tailwindcss/vite": "catalog:",
    "@testing-library/jest-dom": "catalog:",
    "@testing-library/vue": "catalog:",
    "@vitejs/plugin-vue": "catalog:",
    "cross-env": "catalog:",
    "happy-dom": "catalog:",
    "rollup-plugin-visualizer": "catalog:",
    "sass-embedded": "catalog:",
    "tailwindcss": "catalog:",
    "vue-tsc": "catalog:"
  }
}
